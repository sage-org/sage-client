const Spy = require('./src/engine/spy');
const SparqlIterator = require('./src/sparql-iterator');
const SageRequestClient = require('./src/utils/sage-request-client');

module.exports = {
    Spy: Spy,
    SparqlIterator: SparqlIterator,
    SageRequestClient: SageRequestClient
}
